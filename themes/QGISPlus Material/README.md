# QGISPlus Material

本主题基于 Qt Advanced Stylesheets 的 `qt_material` 设计语言，针对 QGIS 和桌面端紧凑布局重新编写。主题保留 QGIS 的原生 `QStyle` 与控件事件处理，仅通过 QSS、Palette 和 SVG 调整外观。

上游 `qt_material` 样式及图标采用 BSD 2-Clause License，许可证见 `LICENSE.qt_material`。
